[pytest]
testpaths = tests
pythonpath = mcp_server
addopts = -q --strict-markers
markers =
    fast: unit tests with every network boundary mocked (the default)
    live: hits the real Open-Meteo or api.weather.gov endpoints
